import 'package:flutter/material.dart';

class AppColors {
  // Backgrounds
  static const Color background = Color(0xFF121212);
  static const Color surface = Color(0xFF1E1E1E);
  static const Color surfaceVariant = Color(0xFF2C2C2C);
  static const Color cardBackground = Color(0xFF1A1A2E);

  // Brand / Accent
  static const Color primary = Color(0xFF4CAF50);       // MangaHour Green
  static const Color primaryVariant = Color(0xFF388E3C); // Darker Green
  static const Color secondary = Color(0xFF03DAC6);      // Teal (kept)

  // Semantic
  static const Color error = Color(0xFFCF6679);
  static const Color star = Color(0xFFFFC107);           // Rating stars

  // Text
  static const Color textPrimary = Color(0xFFE1E1E1);
  static const Color textSecondary = Color(0xFFA0A0A0);
  static const Color textHint = Color(0xFF606060);

  // Utility
  static const Color divider = Color(0xFF303030);
  static const Color shimmerBase = Color(0xFF2A2A2A);
  static const Color shimmerHighlight = Color(0xFF3A3A3A);
  static const Color overlay = Color(0x80000000);        // 50% black
}
